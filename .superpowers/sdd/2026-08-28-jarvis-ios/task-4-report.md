# Task 4 Report — Swift Protocol, Keychain, and Bridge Client

## Status

Task 4 is **implementation-ready / cloud-validation-pending**. It is not marked
complete because this Windows host has no Swift or Xcode toolchain. The required
Codemagic/macOS `swift test` gate remains open for Task 7.

Implementation commit:
`8c3231f19859abf856d7f129a432873f6781dbb2`
(`feat: add Swift Jarvis bridge client`).

Static-review corrective commit:
`6aad81b` (`fix: harden Swift bridge wire protocol`).

## Files

- `ios/Package.swift`
- `ios/Sources/JarvisProtocol/BridgeModels.swift`
- `ios/Sources/JarvisProtocol/RequestSigner.swift`
- `ios/Sources/JarvisCore/KeychainDeviceStore.swift`
- `ios/Sources/JarvisCore/BridgeClient.swift`
- `ios/Tests/JarvisProtocolTests/BridgeModelsTests.swift`
- `ios/Tests/JarvisProtocolTests/RequestSignerTests.swift`
- `ios/Tests/JarvisCoreTests/BridgeClientTests.swift`

No Task 5–8 project, UI, voice, intent, widget, XcodeGen, or Codemagic files were
created, and the SDD ledger was not changed.

## Protocol and security decisions

- `BridgeRequest`, `BridgeResponse`, `RequestKind`, `TaskState`, and `Risk` use
  the exact Python snake-case field names and wire values. Unknown enum values
  throw `BridgeProtocolError.unknownEnumValue`; they never become a success or
  other default state.
- Request payloads use a typed recursive `JSONValue`. `canonicalData()` uses a
  purpose-built ASCII JSON serializer rather than Foundation's presentation
  choices: object keys are sorted in Python code-point order via UTF-8 lexical
  order, separators are compact, `/` remains unescaped, controls use Python's
  short/lowercase escapes, BMP Unicode uses lowercase `\uXXXX`, and non-BMP
  Unicode is emitted as lowercase UTF-16 surrogate pairs.
- `RequestSigner` requires an exact 32-byte secret and generates CryptoKit
  HMAC-SHA256 as exactly 64 lowercase hexadecimal characters. The checked
  fixture was generated by the Task 1 Python implementation.
- The signed wire envelope is assembled directly from the exact canonical
  request bytes supplied to HMAC. It is never reconstructed through
  `JSONEncoder`, preventing number spellings such as `1.0`, exponent forms,
  and `-0.0` from drifting between the signed payload and transmitted request.
- `KeychainDeviceStore` keeps device ID, device secret, and certificate
  fingerprint as three generic-password items. Both add and update paths apply
  `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`. The Security calls are
  behind an injectable wrapper used by deterministic tests.
- `DeviceCredentials`, `KeychainItem`, `BridgeRequest`, `PairingPayload`, and
  `BridgeClient` provide redacted descriptions so a debug print does not expose
  payload content, device secret, certificate fingerprint, or pairing proof.
- The secure production client initializer creates its transport through a
  `PinnedTransportFactory` using the stored fingerprint. The test initializer
  accepts an injected `BridgeTransport`. `PinnedCertificateDelegate` accepts
  only a server-trust challenge whose leaf DER SHA-256 matches the scanned
  lowercase fingerprint; mismatch, missing/malformed trust, and every other
  authentication method are cancelled. Its task-delegate redirect callback
  cancels every HTTP redirect, including 307/308, before a POST body can be
  replayed to a different URL.
- `BridgeRequest`, `BridgeResponse`, `PairingPayload`, and `PairClaimResponse`
  compare decoded top-level keys to their exact wire schemas and reject extras.
  Recursive `payload` dictionaries remain deliberately open for protocol
  payload evolution.
- Bridge URLs must use HTTPS, contain no credentials/query/fragment/base path,
  and resolve syntactically to an approved private/loopback/link-local address
  or local hostname. Public IPv4/IPv6, wildcard-style public values, and legacy
  integer/hex numeric host forms are rejected; there is no HTTP fallback.
- `submit`, pairing claim, confirmation, and cancellation send once. Ambiguous
  timeout, connection-loss, or cancellation errors become `.resultUnknown` and
  are never automatically resubmitted. Only authenticated `GET` task status is
  eligible for the bounded safe-read retry policy.
- Status, confirm, and cancel requests bind the signed device owner and
  `target_request_id` to the URL path. Cancellation intentionally uses the
  Python server's `POST /v1/tasks/{request_id}/confirm` route with request kind
  `cancel`.
- Pairing claim creates a transport from the QR fingerprint before sending the
  unsigned one-time claim. Returned URL-safe Base64 credentials are validated
  as version 1, nonempty device ID, and exactly 32 secret bytes before anything
  is written to Keychain.

## Test-first evidence and inventory

The package manifest and all three test files were written while `ios/Sources`
was absent. A source inventory recorded `ios/Sources is absent` before the
production files were added. The local RED command could not start because the
host has no Swift executable; therefore executable RED/GREEN evidence is
deferred together with the final Swift gate rather than fabricated.

There are 32 Swift test methods:

- 11 model tests: Python canonical fixture parity including nested `1.0`,
  decimals, exponents, and negative zero; nested ordering; slash, control,
  CJK, emoji/surrogate escaping; exact Codable keys; unknown enum rejection;
  strict `BridgeRequest`/`BridgeResponse`/`PairingPayload` top-level fields;
  deliberately open nested payloads; and redacted request descriptions.
- 4 signing tests: Python HMAC fixture parity for the original and numeric
  payloads, exact 32-byte secret enforcement, and lowercase 64-hex validation.
- 17 core tests: Keychain add/read/update/delete/accessibility and validation;
  certificate pin match/mismatch/no-trust/unexpected-auth; HTTPS private/local
  URL validation including public IPv6 and legacy numeric hosts; all 307/308
  HTTP/public-IP/different-host redirects for state-changing POST bodies;
  production pin-factory wiring; request URL/signature and extraction of the
  exact embedded canonical bytes; state-changing single-send `.resultUnknown`;
  safe status retry; pinned pairing and persistence ordering; malformed and
  unknown-field pairing responses; confirmation/cancellation owner, target,
  and path binding.

## Local commands and results

All commands ran from
`C:\Users\Administrator\Documents\ChatGPT\工作项目\.worktrees\jarvis-ios`.

| Command | Result |
| --- | --- |
| `swift --version` | Could not run: `swift` is not recognized on this host. |
| `cd ios; swift test` (before and after implementation) | Could not start for the same missing-toolchain reason. No Swift pass result is claimed. |
| `$env:PYTHONPATH='src'; py -3.12 -m pytest -q --basetemp=<task-temp>` (baseline) | `184 passed in 17.79s`. |
| `$env:PYTHONPATH='src'; py -3.12 -m pytest -q --basetemp=<task-temp>` (after implementation) | `184 passed in 16.11s`. |
| `py -3.12 -m ruff check src tests` | `All checks passed!`. |
| `git diff --cached --check` before the implementation commit | Passed with no whitespace errors. |
| Required-file and out-of-scope path inventory | All 8 Task 4 files present; no Task 5–8 iOS path found. |
| Production-source scan for force unwraps, `fatalError`, `try!`, `as!`, `UserDefaults`, logging calls, and `http://` | No findings. |
| `git show --check --oneline --stat 8c3231f19859abf856d7f129a432873f6781dbb2` | Passed; commit and stat rendered without whitespace errors. |
| Python numeric canonical/HMAC fixture check | Matches `1.0`, decimal, exponent, and `-0.0` canonical JSON and HMAC `10e5b6f4e96399c9ae514dbd3ca8fbc3e1ab81f901dd3f918ac108be7f5fcc89`. |
| `$env:PYTHONPATH='src'; py -3.12 -m pytest -q --basetemp=<task4-review-temp>` (static-review correction) | `184 passed in 14.77s`. |
| `py -3.12 -m ruff check src tests` (static-review correction) | `All checks passed!`. |
| `git diff --check` and `git diff --cached --check` before `6aad81b` | Passed with no whitespace errors. |
| Static source check after correction | Legacy `SignedBridgeRequest`/encoder envelope code absent; redirect-rejection hook and four strict top-level-model checks present. |

These checks prove repository hygiene and Python non-regression only. They do
not prove that Swift compiles or that the Swift tests pass.

## Codemagic/macOS completion gate

Task 7 must run this exact command on a macOS runner with Swift 6:

```bash
cd ios && swift test
```

The gate closes only when both package targets compile and all 32 tests pass.
Any Swift compiler, concurrency, Security/CryptoKit/Network availability,
canonicalization fixture, or test failure must return to Task 4 for correction.
Until that evidence exists, Task 4 remains cloud-validation-pending.
